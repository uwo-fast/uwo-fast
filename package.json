{
  "name": "uwo-fast-lab-signs",
  "private": true,
  "version": "0.1.0",
  "description": "Build FAST lab sign PDFs from Markdown sources.",
  "scripts": {
    "build:lab-signs": "python3 scripts/build_lab_signs.py --all"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.16.0"
  }
}
